import React from 'react';
import { View } from 'react-native';

/**
 * Empty placeholder screen for the FAB button in tab layout
 * This file exists to prevent the "No route named 'placeholder' exists" error
 */
export default function PlaceholderScreen() {
  // Just return an empty view - this screen should never be shown
  return <View />;
}